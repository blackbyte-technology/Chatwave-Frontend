import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Data Deletion Instructions | ChatWave",
  description:
    "Learn how to request deletion of your personal data and ChatWave account. BlackByte Technology respects your privacy and provides a clear data deletion process.",
};

export default function DataDeletionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
